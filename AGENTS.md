Details about the project can be found in README.md

This project uses `yarn` to manage dependencies. Always use `yarn install` or `yard add` and such commands when trying to work with this workspace. Never use `npm install` or such commands.
